Use this command from dockerfile to start server directly from css instead:

FROM solidproject/community-server:7

ARG CONFIG_SERVER
ARG LOG_LEVEL
ARG BASE_URL

ADD $CONFIG_SERVER /tmp/config.json

ENV LOG_LEVEL=$LOG_LEVEL
ENV BASE_URL=$BASE_URL

ENTRYPOINT []
CMD [ "/bin/sh", "-c", "node bin/server.js -c /tmp/config.json -b ${BASE_URL} -l ${LOG_LEVEL} -f /data" ]


First start default server with default config
Then add derived resources component without adding the CONFIG_SERVER
Then with derived resource components configs activated
Finally with activated and custom mapper.

Next:

Try to fix the extensionbased mapper as currently it does not recognise .nq files. Fix: Add customTypes. But this
is JSON, which needs a hint. This hint is added, but due to derived-resource-component it is using
the non-local version of css. 

Idea: Just use the derived resource component repo, as that seems to work.